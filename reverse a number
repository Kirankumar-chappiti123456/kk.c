#include <stdio.h>
int main()
{
  int n=1234, r = 0;

  printf("Enter a number to reverse\n");


  while (n != 0)
  {
    r = r * 10;
    r = r + n%10;
    n = n/10;
  }

  printf("Reverse of the number = %d\n", r);

  return 0;
}
